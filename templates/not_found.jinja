<!DOCTYPE html>
<html>

<head>
  <meta charset="UTF-8">
  <title>List Example</title>
  <!-- Tailwind CSS -->
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>

<body>
  <div class="container mx-auto my-5">
    <h1 class="text-3xl font-bold mb-3">{{ header }}</h1>
    <h2>{{ subheader }}</h2>
    <ul class="list-disc list-inside">
      {% for item in items %}
      <a href="/classes/{{page}}{{item}}">
        <li>{{ item }}</li>
      </a>
      {% endfor %}
    </ul>


  </div>
</body>

</html>
